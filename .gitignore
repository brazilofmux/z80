# Build artifacts
*.o
*.obj
*.d

# Binaries we build (keep the .c sources)
z80-monster
tools/mk*

# Future generated test binaries
tests/*.com
tests/*.bin

# But always keep C sources
!**/*.c
!**/*.h

# Generated test programs
tests/*.com
tests/*.bin

# Editor / OS noise
.DS_Store
*~
*.swp
*.swo
.idea/
.vscode/

# Future DBT / emulator outputs we may generate
*.log
*.trace
perf.data*
callgrind.out.*
# CP/M software stays local — bring your own copies. Only the freely
# redistributable zexdoc/zexall exercisers are tracked.
disks/*/
!disks/zex/

# mdfix -i backups
*.bak

# Host-side unit test binaries
tests/video_test
tests/render_test
tests/*.dSYM/
