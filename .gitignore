# Build artifacts
*.o
*.obj

# Binaries we build (keep the .c sources)
z80-monster
tools/mkhello
tools/mkblock
tools/mkcb

# Future generated test binaries
tests/*.com
tests/*.bin

# But always keep C sources
!**/*.c
!**/*.h

# Generated test programs
tests/*.com
tests/*.bin

# Editor / OS noise
.DS_Store
*~
*.swp
*.swo
.idea/
.vscode/

# Future DBT / emulator outputs we may generate
*.log
*.trace
perf.data*
callgrind.out.*
# Commercial CP/M software stays local — bring your own copies.
disks/zork1/
disks/mscobol/
